{
  "name": "SEO Company in XYZ",
  "description": "You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. You can install the package via composer. Link -  https://thisisatest.com/",
  "keywords": [
    "SEO Company in xyz",
    "best seo",
    "another keyword"
  ],
  "homepage": "https://google.com",
  "version": "1.1"
}
